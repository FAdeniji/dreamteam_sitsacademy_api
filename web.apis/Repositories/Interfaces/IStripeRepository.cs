using System;
using data.models;

namespace web.apis
{
    public interface IStripeRepository
    {
        // Task<Product> AddProduct(string name, string type, string _stripeConfigurationKey);
        // Task<Price> AddProductPrice(string productId, long amount, string currency, string _stripeConfigurationKey, PriceRecurringOptions priceRecurringOptions = null);
        // Task<Product> UpdateProduct(string productId, string name, string description, string _stripeConfigurationKey, List<string> images = null);
        // IEnumerable<Product> GetAllProducts(bool isActive, string _stripeConfigurationKey, List<string> ids = null);
        // Task<Price> UpdateProductPrice(string productId, string priceId, int newAmountInCents, string _stripeConfigurationKey);
    }
}

